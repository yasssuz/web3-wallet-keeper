import styled from "@emotion/styled";
import {
  Container as ContainerImported,
  ButtonsArea as ButtonsAreaImported,
} from "../has-no-wallets/styles";

export const Container = styled(ContainerImported)``;

export const ButtonsArea = styled(ButtonsAreaImported)``;
